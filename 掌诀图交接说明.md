# 掌诀图交接说明

## 项目背景

「神秘学手册」中的小六壬体验课（index.html 内 `id="xlr"` 的 section）需要一张**掌诀图**，展示六宫在左手手指上的位置关系。

## 当前状态

我（前一个 AI）用 SVG 画了一个简版（在 index.html 中搜索 `<svg`，位于 `id="xlr"` 的 section 内），问题：
- 手掌轮廓太粗糙，看起来不像手
- 六宫圆圈的排版不够直观
- 整体美感不符合 9味暗色调的审美标准

**你可以直接删掉那个 SVG 重画，不需要在它基础上修改。**

## 需要画什么

一张**左手掌诀示意图**，标注小六壬六宫在手指关节上的位置：

| 宫名 | 手指位置 | 五行 | 吉凶 |
|------|----------|------|------|
| 大安 | 食指根部 | 木 | 吉 |
| 留连 | 食指指尖 | 水 | 凶 |
| 速喜 | 中指指尖 | 火 | 吉 |
| 赤口 | 无名指指尖 | 金 | 凶 |
| 小吉 | 无名指根部 | 水 | 吉 |
| 空亡 | 中指根部 | 土 | 凶 |

六个位置呈**环形顺时针排列**：食指根→食指尖→中指尖→无名指尖→无名指根→中指根。

## 视觉要求

1. **暗色背景适配**：页面背景是 `#14161b`，图内元素用半透明白/金色调
2. **金色高亮**：当前选中或重点宫位用 `#c9a227` 强调
3. **克制风格**：不要花哨装饰，9味审美的"低长调+发丝线"
4. **尺寸**：宽度不超过 480px，居中放置
5. **格式**：内联 SVG（不用外部图片文件），嵌入 `index.html` 的 xlr section 内

## 可选增强（如果做得好）

- 六个宫位之间画虚线箭头表示顺时针方向
- 吉凶用颜色区分（吉=暖色，凶=冷色）
- 每个宫位旁标注五行

## 技术约束

- 嵌入位置：index.html 中搜索 `<svg`，找到现有的 SVG 元素，整体替换
- 替换后跑 `node --check` 确认 JS 语法不受影响（SVG 在 HTML 区域，不影响 JS，但保险起见）
- 不需要改 JS 逻辑，只需要换掉 SVG 元素

## 建议做法

可以用 Python 脚本或 Write 工具直接替换 SVG 元素。替换后刷新浏览器（http://127.0.0.1:8399/index.html 打开小六壬课）目视检查效果。
