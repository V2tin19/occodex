# 验收清单 —— 每轮开发结束必跑

> **执行方式**：在 D:\gitstuff\1999Occ 运行
> ```
> python tools/check.py
> ```
> 输出 `0 FAIL` 才允许在工作日志记"完成"；有 FAIL 必须先修复或在"疑点"节记录原因。

## check.py 自动覆盖的检查项

1. **语法门**：全部 HTML 的内联 `<script>` 过 `node --check`
2. **算法库**：`js/lib/*.js` 过 `node --check`
3. **回归**：`node tools/regress.js`（六壬起课锚点：韩太守占祈雪→三传巳戌卯/比用知一）
4. **裸声明扫描**：HTML 顶层裸 `const/let` 数量报告（防跨脚本块重复声明事故）
5. **服务器**：8399 端口可达

## check.py 不覆盖、必须人工（AI）做的

6. **浏览器实操**：改动哪个页面，就在浏览器里把该页一个完整交互流程点通（如：起课器选干支→点起课→读结果；导航切换章节；主题切换）
7. **引文纪律**：新增引文能在 classics/ 或 E:\0ccultism 古籍库检索到原文
8. **边界声明**：流派分歧处有声明；不新增实时占卜功能

## 工程纪律（红线）

- **算法唯一副本**：排盘/起课等纯算法只允许存在于 `js/lib/*.js`，页面禁止内嵌第二份副本（引库使用）
- **IIFE 包裹**：所有页面内联脚本用 `(function(){ ... })();` 包裹，禁止顶层裸声明——这是 liuren-proto 事故（两个 script 块重复 const 声明导致整块脚本死亡）的直接教训
- **静态资源缓存**：python http.server 有启发式缓存，改版后浏览器需强刷；引外部库可带 `?v=N` 穿透，改版时递增 N
- **DOM 真相原则**：浏览器探针（evaluate）运行在隔离世界，看不到页面 JS 全局；验证页面状态一律以 DOM 变化为准（推演器头部 `#errBox` 常驻全局错误捕获，自动验收可读取）

## 队列中的治理任务

- [ ] 抽取 `js/lib/paipan.js`（八字四柱排盘+大运，index.html 内嵌版为源）并让 index.html 引库
- [ ] index.html 内联脚本 IIFE 化（383 处顶层声明在函数/IIFE 内，需逐一确认后包裹）
- [ ] 清理根目录调试残留：`_test.html`、`_test2.html`、`_dump2.html`（确认无人引用后删除）
